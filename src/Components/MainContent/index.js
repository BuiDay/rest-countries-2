import React from 'react';
import SearchAndFilter from './SearchAndFilter'

const MainContent = () => {
    return (
        <div>
            <SearchAndFilter />
        </div>
    );
};

export default MainContent;